#include <stdlib.h>
#include <stdio.h>
/**
* main - prints the name of the file
* @argc: offset count
* @argv: offset value
* Return: 0
*/

int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
